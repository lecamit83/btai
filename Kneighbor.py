def UserKNB (D) : 
    length = len(D);
    
    for i in range(0, length): 
        row = []
        for j in range(0, length):
            tu = 0;
            
            if i == j : 
                row[j] = 0;
            elif :
                for k in range(0, len(D[0])):
                    if D[k] == 0 :
                        continue
                    (D[i] * D[j]) 